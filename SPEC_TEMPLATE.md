# Спецификация фичи: [НАЗВАНИЕ]

> Шаблон по Spec-First Methodology. Заполнить все разделы перед началом реализации.
> Неполная спецификация = субагент будет задавать вопросы.

---

## Описание
[Что делает, для кого, зачем. 2–3 предложения.]

**Затронутые модули:** [habr.js / hh.js / filter.js / telegram.js / sheets.js / database.js / poller.js]
**Фаза:** [1 — Хабр / 2 — HH / 3 — опциональная]
**Зависимости:** [на что влияет, что требует]

---

## User Stories

1. Как [роль], я хочу [действие], чтобы [результат]
2. Как [роль], я хочу [действие], чтобы [результат]
3. Как [роль], я хочу [действие], чтобы [результат]

*(Минимум 3 истории. Основной сценарий + крайние случаи)*

---

## Модель данных

### Новые поля / таблицы (если есть)

```sql
-- Таблица: applications (пример добавления поля)
ALTER TABLE applications ADD COLUMN IF NOT EXISTS new_field text;

-- Новая таблица (если нужна)
CREATE TABLE IF NOT EXISTS new_table (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  field_name  text NOT NULL,
  created_at  timestamptz DEFAULT now(),
  UNIQUE (field1, field2)
);
```

**Типы:** uuid, text, jsonb, timestamptz, numeric, boolean, int
**Constraints:** NOT NULL, DEFAULT, CHECK, UNIQUE, FK REFERENCES
**RLS:** [нужна ли политика]

---

## Логика / Алгоритм

```
Шаг 1: [что происходит]
Шаг 2: [условие] → [действие A] / [действие B]
Шаг 3: [результат]
```

**Изменения в фильтрации:** [да/нет. Если да — расписать изменения в таблице решений]
**Изменения в API-вызовах:** [какие эндпоинты добавляются/изменяются]

---

## Интерфейс (если затронут Telegram или Sheets)

### Telegram
```
[Пример карточки или алерта]
```

### Google Sheets
| Колонка | Тип | Источник |
|---|---|---|
| [название] | string/number | [поле объекта] |

---

## Переменные окружения (если добавляются)

```
# .env.example
NEW_VARIABLE=                    # Описание, формат значения
NEW_VARIABLE_2=default_value    # Описание (необязательная)
```

---

## Крайние случаи

| Ситуация | Ожидаемое поведение |
|---|---|
| [поле отсутствует / null] | [вернуть null / использовать дефолт / залогировать warn] |
| [API недоступен] | [вернуть [] / throw / retry] |
| [данные в неожиданном формате] | [парсить по fallback / вернуть null] |
| [дублирующий запрос] | [тихо пропустить / обновить / ошибка] |

---

## Приоритет и порядок реализации

1. [ ] Миграция БД (если нужна) — `database-architect`
2. [ ] Основная логика — `backend-engineer` / `habr-parser` / `hh-api-client`
3. [ ] Интеграция в poller — `backend-engineer`
4. [ ] Обновить .env.example — самостоятельно
5. [ ] QA ревью — `qa-reviewer`
6. [ ] Обновить TECH_SPEC.md — самостоятельно

---

## Критерии готовности

- [ ] Все user stories покрыты
- [ ] Крайние случаи обработаны (null, недоступность, дубли)
- [ ] Нет console.log, нет хардкода
- [ ] qa-reviewer не нашёл критических проблем
- [ ] TECH_SPEC.md обновлён
